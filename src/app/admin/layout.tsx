import { ReactNode } from 'react';

export const metadata = {
  title: 'Admin Dashboard | Pink Tower',
  description: 'Content Management System for Pink Tower International School',
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return children;
}
